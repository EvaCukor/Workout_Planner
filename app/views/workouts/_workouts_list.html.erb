<% object.each do |workout| %>
  <%= link_to workout_path(workout), remote: true, class: 'listing' do %>
    <span class="glyphicon glyphicon-chevron-right"></span><%= ' ' + workout.name %>
    &nbsp;
    <%= render 'shared/votes', obj: workout, obj_type: 'workout' %>
  <% end %>
  <br />
<% end %>