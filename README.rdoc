# Workout Planner

### Description


**Ruby version:** 2.1.1p76

**Rails version:** 4.1.0

### Database creation and initialization
Ruk `rake db:migrate` and then `rake db:seed` (or just `rake db:setup`) to load the initial data into the Body Parts and Equipments Pieces tables.